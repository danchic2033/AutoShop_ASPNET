@model List<Role>
@{
    ViewData["Title"] = "Роли";
}

<div class="row mt-3">
    

    <div class="d-flex align-items-center justify-content-between" style="width: 100%;">
        <div class="d-grid gap-2 col-6">
            <a class="btn btn-primary" asp-controller="Admin" asp-action="Index" type="button">Заказы</a>
            <a class="btn btn-primary" asp-controller="Admin" asp-action="Users" type="button">Пользователи</a>
            <a class="btn btn-primary" asp-controller="Admin" asp-action="Roles" type="button">Роли</a>
            <a class="btn btn-primary" asp-controller="Admin" asp-action="Products" type="button">Товары</a>
        </div>
        <div class="ms-3">
            <span class="text" style="font-size: 1.5rem;">Роли</span>
        </div>
    </div>

    <div class="col-9 mb-3">
        <div class="text-center text-uppercase text-success mb-5 mt-5">
            <h2 class="row-title">Роли</h2>
        </div>

        @if (Model != null && Model.Count > 0)
        {
            <div class="row justify-content-center mt-3">
                <div class="col-12">
                    <div class="text-end">
                        <a class="btn btn-outline-success mb-2 mt-2"
                           asp-controller="Admin"
                           asp-action="AddRole">
                            Добавить новую роль
                        </a>
                    </div>
                    <table class="table table-responsive table-striped table-hover mb-5">
                        <thead>
                            <tr align="center">
                                <th scope="col">#</th>
                                <th scope="col">Наименование роли</th>
                                <th scope="col"></th>
                            </tr>
                        </thead>
                        <tbody>
                            @for (var i = 0; i < Model.Count; i++)
                            {
                                <tr align="center" class="align-baseline">
                                    <td class="fw-bold">@(i + 1)</td>
                                    <td align="left">
                                        <h5>@Model[i].Name</h5>
                                    </td>
                                    <td>
                                        <a class="btn btn-outline-success"
                                           asp-controller="Admin"
                                           asp-action="DeleteRole"
                                           asp-route-roleId="@Model[i].Id">
                                            Удалить
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                        <tfoot>
                            <tr align="center">
                                <th colspan="2"></th>
                                <th colspan="1">
                                    Общее количество ролей: @Model.Count
                                </th>
                            </tr>
                        </tfoot>
                    </table>
                </div>
            </div>
        }
        else
        {
            <div class="text-center">
                <h2 class="row-title">В хранилище нет ролей!</h2>
            </div>
            <div class="text-center mt-5">
                <a class="btn btn-outline-success mb-2 mt-2"
                   asp-controller="Admin"
                   asp-action="AddRole">
                    Добавить новую роль
                </a>
            </div>
        }

    </div>
</div>
